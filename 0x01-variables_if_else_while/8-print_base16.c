#include <stdio.h>
/**
 * main - Entry
 * Return: each time return 0
 */

int main(void)
{
	char s;

	int t;

	s = 'a';
	t = 0;
	while
		(t < 10) {
			putchar(t + '0');
			t++;
		}
	while
		(s <= 'f') {
			putchar(s);
			s++;
		}
	putchar('\n');
	return (0);
}
